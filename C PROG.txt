#include <stdio.h>

int main() {
    int number;
    
    printf("Enter an integer: ");
    // %d tells scanf to read an integer; &number points to the memory space
    scanf("%d", &number);
    
    int square = number * number;
    printf("The square of %d is %d\n", number, square);
    
    return 0;
}
